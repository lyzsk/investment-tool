收到，四条修正全部吸收：① 数据分 **db_bound**（列名=未来表结构，可整体清除重来）与 **raw**（行情原始流，结算后可删）两层；② 命名纠正——没有"桃哥竞价"，那是**策略竞价模块**（我们自己的、持续迭代的模块，桃哥只是风格学习对象）;③ 竞价重点时段单独设计；④ T 日盘中只用 ≤T-1 晚沉淀的策略，当天没有新桃哥输入。

先看试跑可用的真实素材，再动手：
现在写配置、状态、种子 watchlist 和日结算：
回放跑通了：2 条触发（都是 notify 唤醒类）,2 条未触发。验证细节是否判对：
清掉上一轮试跑的产物，重跑完整链路（回放→结算）:
刚才的编辑把原「待办」段覆盖了，补回来：
试跑脚本全套完成并验证通过。交付清单：

## 已建成（`scripts/taoge-paper/`)

| 文件 | 状态 |
|---|---|
| `quote.mjs` — 行情源抽象：腾讯批量快照（主）→新浪→东财push2（兜底），连续失败3次自动切换，GBK解码 | ✅ |
| `sentinel.mjs` — 哨兵：分时段节流（竞价10s/早盘15s/午后30s/尾盘15s，全天~1000请求≈财联社电报量级）、快照拼bar、竞价捕获、触发引擎、唤醒headless claude;`--replay` 回放模式 | ✅ 回放验证 |
| `paper_settle.mjs` — 日结算：成交bar断言→记账→equity→market_quote_daily | ✅ 验证 |
| `dblog.mjs` — db_bound JSONL（列名=未来表结构）+ sha256哈希链 + holes.log | ✅ |
| `config.json` / `paper_state.json` / `watchlist/20260918.json`（种子，来自9/17晚解读） | ✅ |
| `install_tasks.ps1` — 注册两个计划任务（哨兵09:12/结算15:06,Mon-Fri，唤醒计算机）,**未运行，等你确认** | ⏸️ |
| `README.md` — 架构/数据字典/限流预算/清除与迁移说明 | ✅ |

## 回放验证结果（9/18，全链路）

- 沐曦 buy 触发（09:35 bar 承接 9.67亿>5亿、未深跌），次bar开盘542成交→结算断言通过；国芳、金健 notify 唤醒（两只当天都涨停，正确捕捉）；国芳破位条件正确地没触发
- `market_quote_daily.jsonl` 四行（上证+3票）列名与未来表一致；equity、哈希链、决策文件齐全
- 产物已归档 `rehearsal/20260918/`,state 重置待命

## 修掉的两个真坑（已记入 memory)

1. **kline缓存bar字段是 `vol`（股）不是 `v`（手），且无 amount** — 已统一 `shares()/barAmt()` 归一；这也是为什么沐曦第一轮没触发（成交额恒为0)
2. mkline 的 `b[8]` 成交额时有时无 → 一律缺省估算

## 还剩三件（周日晚彩排前）

1. **桃哥 skill 三模式**(evening=解读+沉淀+产出次日watchlist / checkpoint=被唤醒裁决 / close=复盘对账）
2. **规则种子文件**(R1-R9 带 learned_before，从 pilot-2week 迁移）
3. **周日晚真实彩排**:9/18 视频走完整 视频→ASR→解读→watchlist 管线，产出 9/21 的第一个正式 watchlist

竞价模块的预期/验证逻辑骨架在 sentinel 里（09:24:50 出预期、09:26 验证），但**腾讯竞价时段的字段行为只能周一实盘验证**——代码已按 best-effort 解析+raw全落盘+不符合预期就记洞处理，不会硬编。

下一步你定：是现在就接着把 skill 和规则种子建了，还是你先过一遍脚本再说。